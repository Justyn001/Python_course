class Solution:
    def addTwoNumbers(self, l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:
        # tab1 = [int]
        # tab2 = [int]
        # str1 = ""
        # str2 = ""
        # answer:int
        # answer1 = []
        # test = ListNode()
        # while l1:
        #     tab1.insert(0, l1.val)
        #     l1 = l1.next
        # while l2:
        #     tab2.insert(0, l2.val)
        #     l2 = l2.next
        # for i in range(len(tab1) - 1):
        #     str1 = str1 + str(tab1[i])
        # for j in range(len(tab2) - 1):
        #     str2 = str2 + str(tab2[j])
        # answer = int(str1) + int(str2)
        # answer = str(answer)
        # for i in range(len(answer)):
        #     answer1.insert(0,ListNode(answer[i]))
        # return ListNode(answer1)